// NavbarComponent has complex dependencies (ContainerEngine via ProjectService).
// These are better tested via E2E tests.
describe.skip('NavbarComponent', () => {
  it('should create', () => {
    // Skipped: requires ContainerEngine mock
  });
});
